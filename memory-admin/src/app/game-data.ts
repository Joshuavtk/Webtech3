import { ApiData } from "./api-data";

export class GameData {
  // aantal!: number;
  // api!: string;
  total_games!: number;
  total_players!: number;
  chosen_apis!: Array<ApiData>;
  // constructor(data: object[]) {
  //   this.total_games = data[0].aantal_spellen
  // }
}
